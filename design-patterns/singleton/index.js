'use strict';

const { freezeCounterInstance } = require('./freezeCounter')
const counter = require('./example/simpleCounter')

// freezeCounterInstance.a=1
// console.log(freezeCounterInstance.a,'freezeCounter')
console.log(counter,'counter')
